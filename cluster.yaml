apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: devops-cluster
  region: us-east-1
  version: "1.34"

autoModeConfig:
  enabled: false

managedNodeGroups:
  - name: devops-ng
    instanceType: t3.small
    desiredCapacity: 1
    minSize: 1
    maxSize: 1
    amiFamily: AmazonLinux2023
